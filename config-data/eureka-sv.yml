spring:
  application:
    name: eureka-sv
  cloud:
    discovery:
      client:
        register-with-eureka: false
        fetch-registry: false
    instance:
      lease-renewal-interval-in-seconds: 0

server:
  port: 8761